<div class="max-w-3xl mx-auto space-y-6">
  <%= render "shared/ui/heading",
             text: "Editar usuário",
             size: :xl,
             description: "Altere dados, e-mail ou senha do usuário de backoffice." %>

  <div class="bg-white border border-slate-200 rounded-2xl p-5 space-y-6">
    <%= form_with model: @user,
                  url: backoffice_user_path(@user),
                  method: :patch,
                  local: true do |f| %>
      <%= render "backoffice/users/form", form: f %>

      <div class="mt-6 flex items-center justify-between">
        <%= render "shared/ui/button",
                   text: "Cancelar",
                   variant: :secondary,
                   size: :md,
                   href: backoffice_users_path %>

        <%= render "shared/ui/button",
                   text: "Salvar alterações",
                   variant: :primary,
                   size: :md,
                   type: :submit,
                   form: f %>
      </div>
    <% end %>
  </div>
</div>

